kind: pipeline
name: default

steps:
  - name: test
    image: gradle:6.0.1-jdk11
    commands:
      - gradle test
  - name: docker
    image: plugins/docker
    settings:
      repo: djcass44/jmp
      tags:
        - ${DRONE_BRANCH}
      username:
        from_secret: DOCKER_USERNAME
      password:
        from_secret: DOCKER_PASSWORD
trigger:
  when:
    event: push